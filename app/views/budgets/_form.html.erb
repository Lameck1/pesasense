<div class="form-group">
  <%= form.label :group %>
  <%= collection_select(:budget, :group_id, @groups, :id, :name, { :prompt => 'Choose Group' }, class: "form-control" ) %></div>
<div class="form-group">
  <%= form.label :amount %>
  <%= form.number_field :amount, placeholder: "Amount", id: "amount", class: "form-control" %><br>
</div>
